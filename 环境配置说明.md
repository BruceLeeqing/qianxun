# 千寻项目环境配置说明

## 概述

项目现在支持多环境配置，可以根据不同的运行环境（开发、生产）自动加载相应的配置文件。

## 配置文件结构

### QianXunServer 目录
- `config/app.yml` - 默认配置文件（兼容旧版本）
- `config/app.development.yml` - 开发环境配置
- `config/app.production.yml` - 生产环境配置

### 主项目目录
- `config/app.development.yml` - 开发环境配置
- `config/app.production.yml` - 生产环境配置

## 环境变量配置

### 设置环境变量

**Windows PowerShell:**
```powershell
# 开发环境
$env:APP_ENV="development"

# 生产环境
$env:APP_ENV="production"
```

**Linux/Mac:**
```bash
# 开发环境
export APP_ENV=development

# 生产环境
export APP_ENV=production
```

**Docker 环境:**
```yaml
environment:
  - APP_ENV=production
```

## 配置差异

### 开发环境 (development)
- **MySQL**: 连接本地数据库 (`localhost:3306`)
- **Redis**: 连接本地Redis (`localhost:6379`)
- **OSS**: 使用开发环境存储桶 (`qianxun-dev`)
- **Base URL**: `http://localhost:8083`

### 生产环境 (production)
- **MySQL**: 连接Docker容器数据库 (`mysql:3306`)
- **Redis**: 连接Docker容器Redis (`redis:6379`)
- **OSS**: 使用生产环境存储桶 (`qianxun`)
- **Base URL**: `https://qianxun-chat.com`

## 使用方法

### 1. 开发环境运行
```bash
# 设置开发环境
export APP_ENV=development

# 运行项目
cd QianXunServer
go run main.go
```

### 2. 生产环境运行
```bash
# 设置生产环境
export APP_ENV=production

# 运行项目
cd QianXunServer
go run main.go
```

### 3. 使用Docker Compose
```yaml
# 在docker-compose.yml中添加环境变量
services:
  qianxun-server:
    environment:
      - APP_ENV=production
```

## 配置加载顺序

1. 首先尝试加载 `app.{环境}.yml`
2. 如果环境特定配置文件不存在，回退到 `app.yml`
3. 环境变量可以覆盖配置文件中的值

## 环境变量覆盖

配置文件支持环境变量覆盖，例如：

```yaml
mysql:
  dns: root:${MYSQL_ROOT_PASSWORD:-qianxun123456}@tcp(localhost:3306)/qianxun
```

可以通过设置环境变量来覆盖默认值：
```bash
export MYSQL_ROOT_PASSWORD=your_secure_password
```

## 故障排除

### 1. 配置文件未找到
如果出现配置文件读取错误，请检查：
- 配置文件路径是否正确
- 文件权限是否足够
- 文件编码是否为UTF-8

### 2. 环境变量未生效
确保环境变量设置正确：
```bash
# 检查环境变量
echo $APP_ENV

# 重新设置环境变量
export APP_ENV=development
```

### 3. 数据库连接失败
检查对应环境的数据库配置：
- 开发环境：确保本地MySQL和Redis服务已启动
- 生产环境：确保Docker容器中的数据库服务正常运行

## 最佳实践

1. **开发环境**：使用本地数据库，便于调试和测试
2. **生产环境**：使用容器化数据库，确保服务稳定性
3. **敏感信息**：使用环境变量存储密码等敏感信息
4. **版本控制**：不要将包含敏感信息的配置文件提交到版本控制

## 扩展配置

如需添加新的环境（如测试环境），只需创建对应的配置文件：
- `config/app.test.yml`
- 设置环境变量 `APP_ENV=test`